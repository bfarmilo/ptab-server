{
  "name": "ptab-server",
  "version": "1.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "https://github.com/bfarmilo/ptab-server.git"
  },
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "startMongo": "mongod --bind_ip=$IP --dbpath=../data --nojournal \"$@\" & sudo service redis-server start",
    "resetMongo": "mongod --bind_ip=$IP --dbpath=../data --nojournal --repair",
    "start": "node server.js",
    "startlocal": "node server.js -dev"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "8.6.0"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.15.4",
    "mongodb": "^2.2.33",
    "promise-redis": "0.0.5"
  },
  "devDependencies": {
    "fs-extra": "^4.0.2",
    "node-fetch": "^1.7.3"
  }
}
